<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../layout/head.ejs') %>
</head>
<body>
  <header></header>
  <main>
    <p>You have this many books: <%=myBookShelf.length%></p>
    <a href="/searches">Search For Books!</a>
    <ul>
      <select name="My Bookshelves">
        <%totalBookshelves.forEach(bookshelf => {%>
         <% console.log(bookshelf) %>
        <option value="<%=bookshelf.bookshelf%>"><%=bookshelf.bookshelf%> </option>
       <% }) %>
      </select>
      <% myBookShelf.forEach( book => { %>
        <form action="/addbook" method="post">
          <li>
            <%console.log(book);%>
            <img src="<%=book.image_url %>"/>
            <h2><%= book.title %></h2>
            <h3><%= book.author %></h3>
            <p><%= book.description %></p>
            <p><%= book.isbn %></p>
            <a href="/books/<%= book.id %>">Show details</a>
            <!-- <input type="hidden" name="title" value=<%=book.title %> />
            <input type="hidden" name="author" value=<%=book.author %> />
            <input type="hidden" name="description" value=<%=book.description %> />
            <input type="hidden" name="isbn" value=<%=book.isbn %> />
            <button>add to my favorites</button> -->
          </li></ul>
        </form>
      <% }) %>
  </main>
  <footer></footer>
  
</body>
</html>